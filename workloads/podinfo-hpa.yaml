apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: every
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: every
  minReplicas: 2
  maxReplicas: 4
  metrics:
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: 99
